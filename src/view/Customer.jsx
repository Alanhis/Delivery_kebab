const React = require('react');
const Layout = require('./Layout');

module.exports = function Customer() {
    return (
        <Layout>
            <h1>Личный кабинетик</h1>
        </Layout>
    );
}